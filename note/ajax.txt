1. 没有ajxa 之前，如果要提交一个表单，里面有100个字段的信息需要填写。。比如 性别，年龄，邮箱，号码。。。。
   你必须填写好所有的才能去 submit。。结果发现 邮箱重复，修改又提交。。又发现 号码有错误。。

   有了ajax后，在你填写完 邮箱后，就会去请求，同时你可以填写下一个字段。。一旦邮箱submit有响应，就会在邮箱
 那里进行局部刷新。。。。

  
  ajax的方式，是由于 XMLHttpRequest的出现。。。。


2. html的标签都可以通过 js 中 document来创建，
   已经写好的也可以通过 document来获取--通过id