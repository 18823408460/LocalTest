1. ctrl + alt +c  常量的抽取

2. 设置中，清除缓存，默认是 将  data/data/app包 下保存的所有数据清除掉

3. 屏幕适配 === 代码中用到的 值，全部转 dp ...

4. GC 回收级别：
   虚拟用， 弱引用，软引用

   > android3.0后，GC更倾向于回收 弱引用，软引用，，所以不再推荐用这个方法去 缓存数据到内存，，，，用  LruCache..

   > 缓存是为了稍后可能会用，，比如listview滑动，........ 所以帧动画中用到的bitmap 是没有必要缓存的。。.

   本地缓存: 将imageurl，转为md5值 作为文件名保存。。。